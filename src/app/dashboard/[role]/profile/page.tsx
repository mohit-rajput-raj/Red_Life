import SettingsProvider from '@/components/settings-provider/settings-form'
import React from 'react'

type Props = {}

const Settings = (props: Props) => {
  return (
    <div>
      <SettingsProvider/>
    </div>
  )
}

export default Settings